// MySQL-based authentication module (JavaScript version)
// Re-export from TypeScript auth module for backward compatibility
export { registerUser, loginUser, logoutUser, auth, db } from './auth.ts';